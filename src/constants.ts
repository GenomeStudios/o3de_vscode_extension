// ============================================================================
//  Shared constants.
// ============================================================================

/** This extension's fully-qualified id (publisher.name from package.json). Used as the
 *  cpptools CustomConfigurationProvider id and the C_Cpp.default.configurationProvider value. */
export const EXTENSION_ID = "GenomeStudiosInc.o3de-development-tools";

/** Microsoft's C/C++ extension — Tier 1 C++ IntelliSense (and the debugger). Not published on Open VSX. */
export const CPPTOOLS_EXTENSION_ID = "ms-vscode.cpptools";

/** The clangd extension — Tier 2 C++ IntelliSense. Published on both the VS Code Marketplace and Open VSX. */
export const CLANGD_EXTENSION_ID = "llvm-vs-code-extensions.vscode-clangd";
